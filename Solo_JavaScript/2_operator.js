let a = 1;
console.log(a++);
console.log(a);
console.log(++a);

// NOT !
// AND &&
// OR ||

const a1 = 1;
const b1 = '1';

const equals = a1 === b1;
console.log(equals);
// 같은지 확인 (타입까지 일치하는지 확인)

const equals_1 = a1 == b1;
console.log(equals_1);
// 같은지 확인 (타입까지는 고려하지 않는다)